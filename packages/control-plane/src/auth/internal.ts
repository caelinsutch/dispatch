/**
 * Internal API authentication for service-to-service calls.
 *
 * Re-exports from @dispatch/shared for consistency across packages.
 */

export { generateInternalToken, verifyInternalToken } from "@dispatch/shared";
